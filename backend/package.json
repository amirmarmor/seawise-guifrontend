{
  "name": "sw_guifrontend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "dev": "npm run cpbuild && nodemon index.js",
    "cpbuild": "rm -rf build && cp -r ../client/build ."
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "path": "^0.12.7",
  },
  "devDependencies": {
    "nodemon": "^2.0.14"
  }
}
